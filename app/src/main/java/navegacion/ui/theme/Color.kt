package navegacion.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales basados en el diseño HTML
val PrimaryPurple = Color(0xFF667EEA)
val PrimaryDark = Color(0xFF764BA2)
val Secondary = Color(0xFF2D3748)
val SecondaryDark = Color(0xFF1A202C)

// Colores de estado
val Success = Color(0xFF48BB78)
val Warning = Color(0xFFFFC107)
val Error = Color(0xFFF56565)
val Info = Color(0xFF4299E1)

// Colores de fondo y superficie
val Background = Color(0xFFF7FAFC)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFE2E8F0)

// Colores de texto
val TextPrimary = Color(0xFF2D3748)
val TextSecondary = Color(0xFF718096)
val TextTertiary = Color(0xFFA0AEC0)

// Colores adicionales
val CardBorder = Color(0xFFE2E8F0)
val DividerColor = Color(0xFFCBD5E0)

// Colores para eventos deportivos
val GoalColor = Success
val YellowCardColor = Warning
val RedCardColor = Error
val MatchLive = Error
val MatchScheduled = Info
val MatchFinished = Success

// Gradiente simulado (se usará en Box con Brush)
val GradientStart = PrimaryPurple
val GradientEnd = PrimaryDark
